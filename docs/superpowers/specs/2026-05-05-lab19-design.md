---
title: Day 19 Lab — Vector Store + Feature Store Execution Plan
date: 2026-05-05
scope: Core 100 pts (no bonus)
path: Lite (fastembed + Qdrant in-memory + SQLite Feast + FastAPI)
approach: Notebook-by-notebook, guided
---

# Lab 19 Execution Design

## Context

- Environment: `.venv` + data already set up via `setup-lite.sh`
- `app/search.py` is fully implemented (BM25 + vector + RRF hybrid, 1-based rank)
- 4 notebooks have scaffolding + 1–2 TODOs each to fill in
- Goal: 100/100 core pts — all 4 notebooks run with output cells, screenshots, REFLECTION.md

## Phase 0 — Verify Environment

**Action:** `make verify-lite`
**Pass condition:** smoke test exits 0, no import errors
**Time:** ~5 min

## Phase 1 — NB1: Embeddings & Indexing (15 pts)

**File:** `notebooks/01_embeddings_index.ipynb`

**TODO:** Embed+upsert loop (already scaffolded in `.py` — confirm in `.ipynb`)
- Batch size 64, embed `title + " " + text` per doc
- Upsert `PointStruct(id=int, vector=list[float], payload={doc_id, topic, title})`
- Assert `client.count("lab19").count == 1000`

**Screenshots needed:**
1. Cell 4 output: `Indexed: 1000 vectors`
2. Cell 5 output: top-5 results with scores for keyword query
3. Cell 6 output: paraphrase query returns `cloud`-topic docs

**Pass condition:** assertion `n_indexed == 1000` does not raise

## Phase 2 — NB2: Hybrid Search RRF (25 pts)

**File:** `notebooks/02_hybrid_search_rrf.ipynb`

**TODO:** Implement `search_hybrid(query, top_k=10, rrf_k=60)`
- Spec: `score(d) = Σ_r 1 / (rrf_k + rank_r(d))`, rank is 1-based
- Pull `depth = max(top_k*5, 50)` from each retriever before fusing
- Return `list[SearchHit]` sorted by RRF score desc, length == top_k

**Evaluation:** run golden set (50 queries), compute Precision@10 per mode
- Assert: avg hybrid > avg keyword AND avg hybrid > avg semantic
- Slice table: `mixed` queries → hybrid wins; `paraphrase` → semantic wins; `exact` → BM25 wins

**Screenshots needed:**
1. Avg Precision@10 table (3 modes)
2. Slice-by-type table

## Phase 3 — NB3: FastAPI Latency Benchmark (25 pts)

**File:** `notebooks/03_search_api_benchmark.ipynb`

**TODO:** Latency benchmark loop — 100 queries × 3 modes via HTTP GET `/search`
- Measure server-side `latency_ms` field from response (not client RTT)
- Compute P50/P95/P99 per mode
- 10 warmup queries before timing window

**Pass condition:** hybrid P99 server-side < 50 ms

**Screenshots needed:**
1. Single query response JSON (showing `latency_ms` field)
2. P50/P95/P99 table (3 modes)

## Phase 4 — NB4: Feast Feature Store (20 pts)

**File:** `notebooks/04_feast_feature_store.ipynb`

**TODO:** 100-call P99 latency benchmark for `get_online_features()`
- Loop 100× on `user_id=u_001`, record elapsed per call
- Compute and print P99

**Other cells (pre-scaffolded):**
- Generate Parquet offline data for 3 feature views
- `feast apply` → verify 3 views registered
- `feast materialize-incremental` → log shows rows written
- Single `get_online_features(user_id=u_001)` → valid dict returned
- `get_historical_features()` PIT join → 3 rows × N features DataFrame

**Screenshots needed:**
1. `feast apply` STDOUT (3 views listed)
2. `materialize-incremental` log
3. Online lookup result dict
4. PIT join DataFrame

## Phase 5 — Submission (5 pts)

- Write `submission/REFLECTION.md` (≤200 words): which mode wins on which query type and why; when NOT to use hybrid
- Organize screenshots in `submission/screenshots/` (one per notebook minimum)
- `git add -A && git commit -m "Lab 19 submission — <name>" && git push`
- Submit public GitHub URL to VinUni LMS

## Success Criteria (100 pts)

| Check | Pts |
|---|---:|
| NB1: count == 1000 | 5 |
| NB1: top-5 visible | 5 |
| NB1: paraphrase query → cloud cluster | 10 |
| NB2: RRF formula correct (1-based rank) | 10 |
| NB2: hybrid > kw AND hybrid > sem (avg P@10) | 10 |
| NB2: slice table correct | 5 |
| NB3: valid SearchResponse with latency_ms | 5 |
| NB3: P50/P95/P99 table printed | 10 |
| NB3: hybrid P99 < 50ms | 10 |
| NB4: feast apply → 3 views | 5 |
| NB4: materialize succeeds | 5 |
| NB4: online lookup returns valid dict | 5 |
| NB4: P99 < 10ms | 5 |
| NB4: PIT join → 3 rows × N features | 5 |
| Reproducible end-to-end | 5 |
| **Total** | **100** |
